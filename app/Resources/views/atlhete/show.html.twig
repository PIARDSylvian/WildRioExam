{% extends 'base.html.twig' %}

{% block body %}
    <h1>Atlhete</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ atlhete.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ atlhete.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('atlhete_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('atlhete_edit', { 'id': atlhete.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
